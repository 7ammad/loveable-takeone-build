# dredd.yml
endpoint: "http://localhost:3000"

# Main API spec (OpenAPI 3 is fine)
api_description: "./packages/core-contracts/dist/openapi.json"
# Optional extras:
# path:
#   - "./packages/core-contracts/dist/extra/*.json"

language: nodejs
hookfiles:
  - "./dredd-hooks.js"

reporter:
  - dot
  - markdown

loglevel: debug
